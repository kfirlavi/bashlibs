* integrate skel with bashlibs-tools
* add ebuilds for all libraries
* add cmake-macros
* remove the global cmake-macros loading to the bashlibs-cmake-macros module
* write README for each lib with example of usage
* write blog post with examples
* need to warn when no document found. Now it is just INFO.
